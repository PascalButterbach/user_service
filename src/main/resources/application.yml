server:
  port: 9001


spring:
  application.name: USER-SERVICE


eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defeaultZone: http://localhost:8761/eureka

